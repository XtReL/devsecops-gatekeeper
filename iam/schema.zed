definition user {}

definition repository {
    relation reader: user
    relation writer: user
    relation admin: user

    // Композиция прав (ReBAC)
    permission read = reader + writer + admin
    permission push = writer + admin
    permission merge_pr = writer + admin
}